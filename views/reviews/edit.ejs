<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Review</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  </head>
  <body class="bg-light text-dark">
    <%- include('../partials/nav') %>

    <div class="d-flex justify-content-center align-items-center pt-5">
      <div class="card p-4" style="width: 400px">
        <h1 class="mb-4 text-center">Edit Review</h1>
        <form action="/reviews/<%= review._id %>?_method=PUT" method="POST" enctype="multipart/form-data">
          <div class="mb-3">
            <label for="gameName" class="form-label">Game Name</label>
            <input
              type="text"
              id="gameName"
              name="gameName"
              value="<%= review.gameName %>"
              class="form-control"
              required
            />
          </div>

          <div class="mb-3">
            <label for="rating" class="form-label">Rating (1 to 10)</label>
            <select id="rating" name="rating" class="form-select" required>
              <option value="" disabled>Select rating</option>
              <% for (let i = 1; i <= 10; i++) { %>
                <option value="<%= i %>" <% if (review.rating == i) { %>selected<% } %>><%= i %></option>
              <% } %>
            </select>
          </div>

          <div class="mb-3">
            <label for="comments" class="form-label">Comments</label>
            <textarea
              id="comments"
              name="comments"
              rows="4"
              class="form-control"
              required
            ><%= review.comments %></textarea>
          </div>

          <div class="mb-3">
            <label for="image" class="form-label">Upload New Image</label>
            <input type="file" id="image" name="image" accept="image/*" class="form-control" />
          </div>

          <% if (review.image) { %>
            <div class="mb-3 text-center">
              <p class="mb-1">Current Image:</p>
              <img src="<%= review.image %>" alt="Review Image" class="img-fluid rounded" style="max-width: 100%;" />
            </div>
          <% } %>

          <button type="submit" class="btn btn-primary w-100">Update Review</button>
        </form>
      </div>
    </div>
  </body>
</html>
