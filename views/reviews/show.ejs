<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Review Details</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light text-dark">
    <%- include('../partials/nav') %>

    <div class="d-flex justify-content-center align-items-start pt-5">
      <div class="card p-4" style="width: 500px">
        <h1 class="mb-4 text-center"><%= review.gameName %> Review</h1>
        <h5 class="text-center text-muted mb-4">
          by <%= review.owner.username %>
        </h5>

        <% if (review.image) { %>
        <div class="text-center mb-3">
          <img
            src="<%= review.image %>"
            alt="Review Image"
            class="img-fluid rounded"
            style="max-width: 300px"
          />
        </div>
        <% } %>

        <p><strong>Comment:</strong> <%= review.comments %></p>
        <p><strong>Rating:</strong> <%= review.rating %>/10</p>

        <% if (review.owner._id.equals(user._id)) { %>
        <div class="d-flex justify-content-between mt-4">
          <a href="/reviews/<%= review._id %>/edit" class="btn btn-secondary"
            >Edit</a
          >
          <form
            action="/reviews/<%= review._id %>?_method=DELETE"
            method="POST"
          >
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
        <% } %>
      </div>
    </div>
  </body>
</html>
