<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />

    <title>Document</title>
  </head>
  <body>
    <nav class="navbar bg-dark px-4">
      <a class="navbar-brand text-white" href="/">GameList</a>
      <% if (!session.userId) { %>
      <div class="d-flex gap-3 ms-auto">
        <a class="nav-link text-white" href="/auth/sign-up">Sign up</a>
        <a class="nav-link text-white" href="/auth/sign-in">Sign in</a>
      </div>
      <% } else { %>
      <div class="d-flex gap-3 ms-auto">
        <a class="nav-link text-white" href="/reviews">My Reviews</a>
        <a class="nav-link text-white" href="/reviews/all">All Reviews</a>
        <a class="nav-link text-white" href="/auth/sign-out">Sign out</a>
        <% } %>
      </div>
    </nav>
  </body>
</html>
